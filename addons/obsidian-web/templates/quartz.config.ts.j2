// {{ item.name }} — Quartz v4 configuration
// Generated by Ansible (obsidian-web addon). Do not edit manually;
// this file is overwritten by the build-quartz-sites script before each build.

import { QuartzConfig } from "./quartz/cfg"
import * as Plugin from "./quartz/plugins"

const config: QuartzConfig = {
  configuration: {
    pageTitle: "{{ item.title }}",
    enableSPA: true,
    enablePopovers: true,
    analytics: null,
    locale: "en-US",
    // baseUrl must be the hostname only (no scheme, no trailing slash).
    // Leave empty when serving without a domain (local/dev mode).
    baseUrl: "{{ item.domain | default('') }}",
    // Never publish anything outside the configured _public/<site> directory.
    // These patterns guard against accidentally including system files if
    // Quartz is ever pointed at a broader directory by mistake.
    // Note: filter plugins only act on Markdown — non-Markdown files (canvas,
    // PDF, images) are emitted regardless of ExplicitPublish, so we exclude
    // them here as a mandatory second layer.
    ignorePatterns: [
      ".obsidian",
      ".git",
      "_private",
      "_repos",
      "node_modules",
      "**/*.canvas",   // Obsidian canvas files — binary, not publishable as HTML
      "**/*.pdf",      // PDFs bypass ExplicitPublish filter; exclude explicitly
    ],
    defaultDateType: "modified",
    theme: {
      fontOrigin: "googleFonts",
      cdnCaching: true,
      typography: {
        header: "Schibsted Grotesk",
        body: "Source Sans Pro",
        code: "IBM Plex Mono",
      },
      colors: {
        lightMode: {
          light: "#faf8f8",
          lightgray: "#e5e5e5",
          gray: "#b8b8b8",
          darkgray: "#4e4e4e",
          dark: "#2b2b2b",
          secondary: "#284b63",
          tertiary: "#84a98c",
          highlight: "rgba(143, 157, 193, 0.15)",
          textHighlight: "#fff23688",
        },
        darkMode: {
          light: "#161618",
          lightgray: "#393639",
          gray: "#646464",
          darkgray: "#d4d4d4",
          dark: "#ebebec",
          secondary: "#7b97aa",
          tertiary: "#84a98c",
          highlight: "rgba(143, 157, 193, 0.15)",
          textHighlight: "#b3aa0288",
        },
      },
    },
  },
  plugins: {
    transformers: [
      Plugin.FrontMatter(),
      Plugin.CreatedModifiedDate({
        priority: ["frontmatter", "filesystem"],
      }),
      Plugin.SyntaxHighlighting({
        theme: {
          light: "github-light",
          dark: "github-dark",
        },
        keepBackground: false,
      }),
      Plugin.ObsidianFlavoredMarkdown({ enableInHtmlEmbed: false }),
      Plugin.GitHubFlavoredMarkdown(),
      Plugin.TableOfContents(),
      Plugin.CrawlLinks({ markdownLinkResolution: "shortest" }),
      Plugin.Description(),
      Plugin.Latex({ renderEngine: "katex" }),
    ],
    filters: [
      // ExplicitPublish = opt-in model: only notes with `publish: true` in
      // frontmatter are built. This enforces "private by default" — a note
      // accidentally placed in _public/ will not be published without the flag.
      // (RemoveDrafts would be opt-out: publish everything unless draft:true)
      Plugin.ExplicitPublish(),
    ],
    emitters: [
      Plugin.AliasRedirects(),
      Plugin.ComponentResources(),
      Plugin.ContentPage(),
      Plugin.FolderPage(),
      Plugin.TagPage(),
      Plugin.ContentIndex({
        enableSiteMap: true,
        enableRSS: true,
      }),
      Plugin.Assets(),
      Plugin.Static(),
      Plugin.NotFoundPage(),
    ],
  },
}

export default config
